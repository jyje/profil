{
  "name": "profil",
  "version": "0.1.0-dev.0",
  "private": true,
  "description": "AI가 관리하는 포트폴리오 · 지식기반 · 포지션별 이력서 사이트 템플릿",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'TODO(M4): packages/site 개발 서버 연결'",
    "build": "npm run build:design && npm run build:packages",
    "build:design": "echo 'TODO(M2): tokens.yaml -> CSS vars + reference.docx'",
    "build:packages": "npm run build --workspace=profil",
    "test": "npm run test --workspaces --if-present",
    "profil": "node packages/cli/dist/index.js",
    "check": "npm run build:packages && npm run test --workspaces --if-present && node packages/cli/dist/index.js check"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
