{
  "name": "@profil/cli",
  "version": "0.1.0-dev.0",
  "description": "Profil 내부 CLI — 완전 초기화(init), 정적 검사(check), 산출물 정리(clean)",
  "type": "module",
  "bin": {
    "profil": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -b",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@profil/jari": "0.1.0-dev.0",
    "yaml": "^2.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "gray-matter": "^4.0.3",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
